@model LibraryManagementSystem.Models.ReservationDetails

@{
    ViewData["Title"] = "Create";
}
<h1>Create</h1>

<h4>ReservationDetails</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            @*<div asp-validation-summary="ModelOnly" class="text-danger"></div>*@
            <div class="form-group">
                <label asp-for="ReservedDate" class="control-label"></label>
                <input asp-for="ReservedDate" id="datepicker" type="text" class="form-control" />
                @*<span asp-validation-for="ReservedDate" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <label asp-for="ReturnedDate" class="control-label"></label>
                <input asp-for="ReturnedDate" type="text" class="form-control" />
                @*<span asp-validation-for="ReturnedDate" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
@section Scripts
    {
    <script>
        $(function () {
            $("#datepicker").datepicker({
                minDate: 0,
                onSelect: function () {
                    var dateObject = $(this).datepicker('getDate');
                    dateObject.setDate(dateObject.getDate() + 15)
                    console.log(dateObject)
                    $("#ReturnedDate").datepicker({
                        maxDate: dateObject
                    });
                }
            });

        });
    </script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
